# Prisma (Node) connection to the shared PostgreSQL database.
# NOTE: plain postgresql:// here (NOT the +asyncpg prefix used by ai_service/SQLModel).
# Same database, different driver format. Run `prisma` commands from this backend/ dir.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/grub_group
